{
  "description": "Pre-commit verification gate: blocks git commit on main/master, with RuboCop offenses, or with Brakeman warnings (Rails projects only).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}\"/.claude/hooks/pre-commit-gate.sh",
            "timeout": 300
          }
        ]
      }
    ]
  }
}
