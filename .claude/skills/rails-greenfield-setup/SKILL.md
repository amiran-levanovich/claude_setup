---
name: rails-greenfield-setup
description: Use when initializing a brand-new Rails project or major standalone subsystem from scratch, or when the user says 'new project', 'start from scratch', 'rails new', 'greenfield', or 'bootstrap the app'. Covers requirements gathering, architecture diagrams, data modeling, mandatory tooling, and the sign-off gate before any feature code.
---

Read `agent_docs/building_the_project.md` and follow it. Locate it as follows: use `agent_docs/building_the_project.md` in the project root if present (drop-in install); otherwise read `../../../agent_docs/building_the_project.md` relative to this skill's directory (plugin install).

It is the single source of truth for the greenfield phase sequence (Phases 0–4) and the sign-off gate that must be cleared before feature coding begins.
