---
name: rails-db-migrations
description: Use when creating or editing database migrations, designing schemas, adding indexes or constraints, or defining ActiveRecord associations and enums in a Rails project. Enforces PostgreSQL-safe, zero-downtime migration patterns.
---

Read `agent_docs/database_schema.md` and follow it. Locate it as follows: use `agent_docs/database_schema.md` in the project root if present (drop-in install); otherwise read `../../../agent_docs/database_schema.md` relative to this skill's directory (plugin install).

It is the single source of truth for database-level constraints, enum columns, indexing strategy, and strong_migrations-safe patterns.
