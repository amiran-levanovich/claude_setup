---
name: rails-testing
description: Use when writing or running RSpec specs in a Rails project — system specs, request specs, factories, mocking external services, or speeding up a slow suite.
---

Read `agent_docs/running_tests.md` and follow it. Locate it as follows: use `agent_docs/running_tests.md` in the project root if present (drop-in install); otherwise read `../../../agent_docs/running_tests.md` relative to this skill's directory (plugin install).

It is the single source of truth for the testing hierarchy (system/request specs over unit tests), FactoryBot strategy, mocking rules, and execution commands.
