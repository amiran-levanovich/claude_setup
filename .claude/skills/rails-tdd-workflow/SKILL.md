---
name: rails-tdd-workflow
description: Use when implementing any feature, bugfix, or refactor in a Rails project. Covers branching, the feature-scoped to-do list, the TDD cycle (test-first, commit failing tests, then implement), and the pre-commit verification gate.
---

Read `agent_docs/coding_workflow.md` in the project root and follow it. It is the single source of truth for the day-to-day TDD lifecycle, branch/commit hygiene, and the pre-commit gate.
